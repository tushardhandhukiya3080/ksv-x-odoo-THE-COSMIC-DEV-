// Re-export the generated Prisma client + enums so apps import from one place:
//   import { PrismaClient, Role } from '@vendorbridge/prisma';
export * from '@prisma/client';
export { PrismaClient } from '@prisma/client';
